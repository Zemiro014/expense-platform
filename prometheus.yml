global:
  scrape_interval: 5s # Busca métricas a cada 5 segundos (bom para desenvolvimento)

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      # O Prometheus vai buscar as métricas dentro da rede do Docker na porta 8889 do coletor
      - targets: ['otel-collector:8889']